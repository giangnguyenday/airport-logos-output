# airport-logos-output

Static GitHub Pages export of 100 airport logos.

## Local Preview

Serve this folder with any static server, for example:

```bash
python3 -m http.server 4173
```

Then open `http://localhost:4173`.

## Publish

This repo is intended to be served from GitHub Pages on the `main` branch root.
